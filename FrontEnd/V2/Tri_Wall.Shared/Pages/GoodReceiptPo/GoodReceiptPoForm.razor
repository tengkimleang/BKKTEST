@page "/goodreceptpoform"
@using Tri_Wall.Shared.Views.GoodReceptPo.MobileAppScreen
@rendermode InteractiveWebAssembly
<PageTitle>Good Receipt PO</PageTitle>
<AuthorizeView Context="another_name">
    <Authorized>
        <FluentGrid Spacing="2" OnBreakpointEnter="@UpdateGridSize" AdaptiveRendering="true">
        </FluentGrid>
        @if (_init)
        {
            @if (_isXs)
            {
                <GoodsReceiptPoDashboard />
            }
            else
            {
                <GoodsReceiptPoDefault Token="@another_name.User.FindFirst("token")?.Value" />
            }
        }
    </Authorized>
</AuthorizeView>
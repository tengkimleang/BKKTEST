@page "/ReturnRequest"
@using Tri_Wall.Shared.Views.ReturnRequest.MobileAppScreen
@inherits MvvmComponentBase<ReturnRequestViewModel>
@rendermode InteractiveWebAssembly
@* @attribute [StreamRendering] *@
<PageTitle>Return</PageTitle>
<AuthorizeView Context="another_name">
    <Authorized>
        <FluentGrid Spacing="2" OnBreakpointEnter="@UpdateGridSize" AdaptiveRendering="true">
        </FluentGrid>
        @if (_init)
        {
            @if (_isXs)
            {
                <ReturnRequestDashboard/>
            }
            else
            {
                <ReturnRequestDefault Token="@(another_name.User.FindFirst("token")?.Value)" ViewModel="ViewModel"/>
            }
        }
    </Authorized>
</AuthorizeView>